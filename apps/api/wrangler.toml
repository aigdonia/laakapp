name = "fin-ai-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[triggers]
crons = ["*/5 * * * *"]  # Every 5 minutes — process scheduled notifications

[[d1_databases]]
binding = "DB"
database_name = "fin-ai-db"
database_id = "local"
migrations_dir = "drizzle"
